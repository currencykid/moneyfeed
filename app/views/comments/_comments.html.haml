%h3 Comments
- commentable.comments.each do |comment|
  .well
    = comment.body
    %br
    %small
    - if user_signed_in? && current_user  == comment.user_id
      = link_to "Delete" , comment, method: :delete
