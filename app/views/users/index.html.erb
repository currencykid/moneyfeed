<div class="container" > 
  <div class="col-md-6 col-md-offset-3">
    <h2>Scores</h2>
  </div>

  <hr/> 

  <div class="col-md-6 col-md-offset-3">
    <table class="table table-hover table-condensed">
      <thead>
        <th>User</th>
        <th>Points</th>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td> <%= link_to user.username, user_path(user) %> </td> 
            <td><span class=" glyphicon glyphicon-flash"> </span> <%= user.points%> </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
